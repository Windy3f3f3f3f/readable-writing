# 完整示例：从仪表盘体改成可读正文

来源：脱敏并改造过的合成迁移记录。
命中规则：R01 加粗滥用、R02 箭头链、R03 勾叉状态位、R04 括号栈。机检分数从 4 降到 0。
注意：修改后版本**一个事实和数字都没丢**（见“不发明事实”）。

## 修改前

> 1. **how-works reconcile ✓**:WIP 并非废稿——含独有的 ⚖️ 免责声明段、"clean-room 教育实现"定位重构、去掉危险表述。stash → ff 到 `bc6eb68` → pop 三方合并零冲突 → 独有价值以 commit `3fef66c` 落库(领先 origin 1 commit,**push 留给用户按键**)。
> 2. **PAT 吊销 ✓**:3090 两仓库 remote 去 token + 冻结标记 `MIGRATED-TO-AI-OS.md`;`~/.git-credentials` 残留行删除(备份 .bak-20260702,其余 6 条凭证未动);用 GitHub 公开端点 `POST /credentials/revoke`(无需登录,专为泄漏 token 设计)吊销:吊销前 200 → 202 受理 → 复验 401 死透。

## 修改后

> 1. how-works 的 WIP 核对完毕,不是废稿:里面有独有的免责声明段、"clean-room 教育实现"的定位重构,还去掉了几处危险表述。处理时先 stash,把分支快进到 `bc6eb68`,再 pop 做三方合并,没有冲突;这些独有内容以提交 `3fef66c` 进了仓库。本地现在领先 origin 一个 commit,push 留给用户来按。
> 2. PAT 吊销完成。3090 上两个仓库的 remote 去掉了 token,并放了冻结标记 `MIGRATED-TO-AI-OS.md`;`~/.git-credentials` 里的残留行删掉了(留有备份 .bak-20260702,其余 6 条凭证没动)。吊销走的是 GitHub 公开端点 `POST /credentials/revoke`——它无需登录,专为泄漏 token 设计。吊销前该 token 还能拿到 200,请求返回 202 受理,复验时已经是 401,吊销确认生效。

## 改动清单(打磨报告示范)

- 去掉全部 5 处粗引头与 ✓/⚖️ 状态位,结论改由句子承载(R01/R03);
- 两条箭头链「stash → ff → pop → 落库」「200 → 202 → 401」展开为带谓语的时序句(R02);
- 「(备份 .bak-20260702,其余 6 条凭证未动)」保留为单层短括注;「(无需登录,专为泄漏 token 设计)」提为独立分句(R04);
- 所有 commit hash、文件名、数字逐一保真,零增删。
