# 合法术语与结构样例（应全部放行，0 findings）

实验共招募 42 名被试，其中 6 名被试的数据因设备故障剔除。问卷由被访者独立填写。

召回率显著提升：从 61.2% 到 78.4%（见图3）。P95 延迟从 120ms 降到 80ms，降幅明显。

`primary` 字段保留 schema 原名；`organic` 枚举值在导出时映射为中文标签。数据集经过归一化（normalize）处理，后文统一说"归一化"。

fallback path 的兜底逻辑写进了 systemd unit:主服务失败时回退到只读模式。SSH 已连通，备份脚本改指新路径。

版本从 v0.8 迁移到 v0.9，错误率 26%→11%。
